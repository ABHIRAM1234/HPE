apiVersion: v1
kind: Service
metadata:
    name: "aks-5da5"
    labels:
        app: "aks-5da5"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aks-5da5"